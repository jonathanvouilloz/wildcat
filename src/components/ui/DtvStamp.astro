---
/**
 * DtvStamp â€” badge DTV officiel (asset /assets/dtv-stamp.webp : tigre +
 * Â« DTV Visa Friendly Â», fond transparent, 627Ã—627).
 * Remplace l'ancien sceau dessinÃ© en CSS (motif maquette).
 * Texte de marque, JAMAIS traduit (comme Â« Wildcat Â») â€” voir docs/DESIGN.md.
 * DÃ©coratif : alt vide, le contexte textuel vit dans la section parente.
 */
export interface Props {
  /** Largeur/hauteur en px (asset carrÃ©). Maquette landing : 240 Â· styleguide : 150. */
  size?: number;
  class?: string;
}
const { size = 150, class: extra = '' } = Astro.props;
---

<img
  class:list={['dtv-stamp', extra]}
  src="/assets/dtv-stamp.webp"
  alt=""
  width={size}
  height={size}
  loading="lazy"
  decoding="async"
/>

<style>
  .dtv-stamp {
    flex: 0 0 auto;
    /* drop-shadow (suit le contour alpha) â€” Ã©quivalent du box-shadow de l'ancien sceau */
    filter: drop-shadow(0 18px 32px rgba(0, 0, 0, 0.4));
  }
</style>

<!-- hmr-probe -->


