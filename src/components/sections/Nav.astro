---
import Button from '../ui/Button.astro';
import ArrowWild from '../ui/ArrowWild.astro';

/**
 * Nav — mega menu WildCat (repris du bundle Claude Design, adapté au design system).
 * Tokens (forest #2E5D3C, gold, cream), Satoshi (titres+corps), Fraunces (.em accents).
 * CTA Join = <Button> gold ; CTA cartes promo = pill gold + swoosh.
 * Desktop : mega panels au survol/clic/clavier. Mobile (<1080px) : drawer accordéon.
 *
 * Props :
 *  - sticky (def. true) : position sticky ; false = relative (démo styleguide).
 *  - cta : libellé du CTA Join (sinon déduit de la page courante).
 */
export interface Props {
  sticky?: boolean;
  cta?: string;
}
const { sticky = true, cta } = Astro.props;

// CTA dynamique selon la page (toujours avec le swoosh).
function ctaFor(p: string): string {
  if (p.includes('/dtv-visa')) return 'Start your DTV';
  if (p.includes('/stay-train')) return 'Plan your stay';
  if (p.includes('/classes') || p.includes('/schedule') || p.includes('/pricing')) return 'Book a class';
  return 'Join now';
}
const ctaLabel = cta ?? ctaFor(Astro.url.pathname);
---

<!-- ============ ICON SPRITE ============ -->
<svg width="0" height="0" style="position:absolute" aria-hidden="true">
  <defs>
    <g id="i-glove"><path d="M8 7h6a4 4 0 0 1 4 4v1.5a3 3 0 0 1-3 3h-1.5" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/><path d="M8 7a3 3 0 0 0-3 3v3a3 3 0 0 0 3 3h5.5V7z" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/><path d="M13.5 16v1.5a2 2 0 0 1-2 2H9a2 2 0 0 1-2-2V16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/></g>
    <g id="i-levels" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"><path d="M6 18v-5M12 18V6M18 18v-8"/></g>
    <g id="i-user" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="3.4"/><path d="M5.5 19a6.5 6.5 0 0 1 13 0"/></g>
    <g id="i-users" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="8.5" r="2.8"/><circle cx="16.5" cy="9.5" r="2.2"/><path d="M4 18a5 5 0 0 1 10 0M14.5 18a4 4 0 0 1 5.5-3.7"/></g>
    <g id="i-kids" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="8" cy="8" r="2.2"/><circle cx="16" cy="8" r="2.2"/><path d="M5 18v-3a3 3 0 0 1 6 0v3M13 18v-3a3 3 0 0 1 6 0v3"/></g>
    <g id="i-flame" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 4c1.2 3.6-1.8 4.8-1.8 7.8A3.6 3.6 0 0 0 15.6 15c1.2 3-.6 5-3.6 5s-7.2-2.4-7.2-7.8c0-4.8 4.8-5.4 7.2-8.4z"/></g>
    <g id="i-calendar" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="5" width="16" height="15" rx="2.5"/><path d="M4 9.5h16M8 3.5v3M16 3.5v3"/></g>
    <g id="i-tag" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M4 12.5V5.5A1.5 1.5 0 0 1 5.5 4h7l7.5 7.5a1.5 1.5 0 0 1 0 2L13 21a1.5 1.5 0 0 1-2 0z"/><circle cx="8.5" cy="8.5" r="1.3" fill="currentColor" stroke="none"/></g>
    <g id="i-spark" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3v4M12 17v4M3 12h4M17 12h4M6 6l2.5 2.5M15.5 15.5 18 18M18 6l-2.5 2.5M8.5 15.5 6 18"/></g>
    <g id="i-bed" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M4 18v-9M4 13h16a0 0 0 0 1 0 0v5M20 18v-3a2 2 0 0 0-2-2"/><path d="M7 13v-2a1.5 1.5 0 0 1 1.5-1.5H13a1.5 1.5 0 0 1 1.5 1.5v2"/></g>
    <g id="i-scooter" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="6" cy="17" r="2.6"/><circle cx="18" cy="17" r="2.6"/><path d="M8.5 17h7M5.5 17l3.5-8h3l4 8M11 9h4.5"/></g>
    <g id="i-pin" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 21c4-4.5 6.5-8 6.5-11A6.5 6.5 0 0 0 5.5 10c0 3 2.5 6.5 6.5 11z"/><circle cx="12" cy="10" r="2.3"/></g>
    <g id="i-map" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 5 4 7v12l5-2 6 2 5-2V5l-5 2-6-2z"/><path d="M9 5v12M15 7v12"/></g>
    <g id="i-id" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="3.5" y="6" width="17" height="12" rx="2.5"/><circle cx="9" cy="11.5" r="2"/><path d="M6.5 16a2.8 2.8 0 0 1 5 0M14 10h4M14 13.5h4"/></g>
    <g id="i-check" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3.5l2.4 1.7 2.9-.2 1 2.8 2.3 1.8-.9 2.8.9 2.8-2.3 1.8-1 2.8-2.9-.2L12 20.5l-2.4-1.7-2.9.2-1-2.8-2.3-1.8.9-2.8-.9-2.8 2.3-1.8 1-2.8 2.9.2z"/><path d="M9 12l2 2 4-4"/></g>
    <g id="i-clock" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="8"/><path d="M12 8v4.5l3 2"/></g>
    <g id="i-help" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="8"/><path d="M9.5 9.5a2.5 2.5 0 0 1 4.8.9c0 1.7-2.3 2.1-2.3 3.6"/><circle cx="12" cy="17" r="0.4" fill="currentColor" stroke="currentColor"/></g>
    <g id="i-book" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M5 5.5A1.5 1.5 0 0 1 6.5 4H18v14H6.5A1.5 1.5 0 0 0 5 19.5z"/><path d="M5 19.5A1.5 1.5 0 0 1 6.5 18H18v2H6.5A1.5 1.5 0 0 1 5 18.5"/></g>
    <g id="i-star" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 4l2.4 4.9 5.4.8-3.9 3.8.9 5.3L12 17.1 7.2 19.6l.9-5.3L4.2 9.7l5.4-.8z"/></g>
    <g id="i-camera" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="3.5" y="7" width="17" height="12" rx="2.5"/><circle cx="12" cy="13" r="3"/><path d="M8.5 7l1.2-2h4.6l1.2 2"/></g>
    <g id="i-heart" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 19.5C6.5 15.5 4 12.5 4 9.5A3.8 3.8 0 0 1 12 7.8 3.8 3.8 0 0 1 20 9.5c0 3-2.5 6-8 10z"/></g>
    <g id="i-chat" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M5 6h14a1.5 1.5 0 0 1 1.5 1.5v7A1.5 1.5 0 0 1 19 16H9l-4 3v-3H5a1.5 1.5 0 0 1-1.5-1.5v-7A1.5 1.5 0 0 1 5 6z"/></g>
    <g id="i-leaf" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M5 19c0-8 6-13 14-13 0 8-5 14-13 14M6 18c3-5 6-7 9-8"/></g>
    <g id="i-route" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="6" cy="18" r="2.2"/><circle cx="18" cy="6" r="2.2"/><path d="M8 18h6a3 3 0 0 0 0-6h-4a3 3 0 0 1 0-6h6"/></g>
    <g id="i-caret" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 8l5 5 5-5"/></g>
  </defs>
</svg>

<!-- ============ UTILITY BAR ============ -->
<div class="util">
  <div class="wrap">
    <div class="u-left">
      <span class="u-item"><svg class="i"><use href="#i-pin"/></svg> Old City, Chiang Mai</span>
      <span class="u-item hide-sm"><span class="dot-open"></span> Open today · 07:00–21:00</span>
      <span class="u-item hide-sm"><svg class="i"><use href="#i-chat"/></svg> +66 81 234 5678</span>
    </div>
    <div class="u-right">
      <div class="u-soc">
        <a href="#" aria-label="Instagram"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="5"/><circle cx="12" cy="12" r="4"/><circle cx="17.5" cy="6.5" r="1" fill="currentColor" stroke="none"/></svg></a>
        <a href="#" aria-label="Facebook"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M14 9V7c0-1 .5-1.5 1.5-1.5H17V2h-3c-2.5 0-4 1.6-4 4v3H7v3.5h3V22h4v-9.5h2.7l.3-3.5H14z"/></svg></a>
        <a href="#" aria-label="YouTube"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M22 8s-.2-1.5-.8-2.1c-.8-.8-1.6-.8-2-.9C16.5 4.7 12 4.7 12 4.7s-4.5 0-7.2.3c-.4 0-1.2.1-2 .9C2.2 6.5 2 8 2 8s-.2 1.7-.2 3.5v1.1c0 1.8.2 3.5.2 3.5s.2 1.5.8 2.1c.8.8 1.8.8 2.3.9 1.7.2 7 .3 7 .3s4.5 0 7.2-.3c.4 0 1.2-.1 2-.9.6-.6.8-2.1.8-2.1s.2-1.7.2-3.5v-1.1C22.2 9.7 22 8 22 8z"/></svg></a>
      </div>
      <span class="lang"><b>EN</b> <span>/</span> FR</span>
    </div>
  </div>
</div>

<!-- ============ MAIN NAV ============ -->
<header class:list={['nav', !sticky && 'nav--static']} id="wc-nav">
  <div class="wrap">
    <a class="brand" href="#" aria-label="WildCat Muay Thai home"><img src="/assets/logo-cream.png" alt="WildCat Muay Thai"></a>

    <nav class="menubar" aria-label="Primary">

      <!-- TRAIN -->
      <div class="mi wide" data-mega>
        <button aria-haspopup="true" aria-expanded="false"><span class="lbl">Train</span> <svg class="caret"><use href="#i-caret"/></svg></button>
        <div class="mega" role="menu">
          <div class="mcol">
            <span class="lab">Classes</span>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-glove"/></svg></span><span class="mtx"><span class="mt">Beginner Muay Thai</span><span class="md">Start from zero — no experience needed</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-levels"/></svg></span><span class="mtx"><span class="mt">All-Levels Classes</span><span class="md">Daily group training for everyone</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-user"/></svg></span><span class="mtx"><span class="mt">Private Sessions</span><span class="md">One-on-one with a Kru</span></span></a>
          </div>
          <div class="mcol">
            <span class="lab">For everyone</span>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-heart"/></svg></span><span class="mtx"><span class="mt">Women's Muay Thai</span><span class="md">A welcoming, supportive space</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-kids"/></svg></span><span class="mtx"><span class="mt">Kids &amp; Family</span><span class="md">Fun, safe, confidence-building</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-flame"/></svg></span><span class="mtx"><span class="mt">Fight Team</span><span class="md">Train hard &amp; compete</span></span></a>
          </div>
          <div class="mcol">
            <span class="lab">Plan your training</span>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-calendar"/></svg></span><span class="mtx"><span class="mt">Class Schedule</span><span class="md">Daily timetable &amp; sessions</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-tag"/></svg></span><span class="mtx"><span class="mt">Pricing &amp; Passes</span><span class="md">Drop-in, monthly &amp; long-stay</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-spark"/></svg></span><span class="mtx"><span class="mt">Free Trial Class</span><span class="md">Try your first class on us</span></span></a>
          </div>
          <div class="mfeat">
            <span class="ft-tag">New here?</span>
            <h4>Your first class is <span class="em">free.</span></h4>
            <p>Never thrown a kick? Perfect. Everyone starts somewhere — come as you are.</p>
            <div class="ff"><a class="fbtn" href="#">Book a trial <ArrowWild class="fbtn-ar" size={20} /></a></div>
          </div>
          <div class="mfoot">
            <div class="links">
              <a href="#"><svg><use href="#i-calendar"/></svg> View full schedule</a>
              <a href="#"><svg><use href="#i-tag"/></svg> Compare passes</a>
            </div>
            <a class="allcta" href="#">All classes <ArrowWild size={18} /></a>
          </div>
        </div>
      </div>

      <!-- STAY & TRAIN -->
      <div class="mi wide" data-mega>
        <button aria-haspopup="true" aria-expanded="false"><span class="lbl">Stay &amp; Train</span> <svg class="caret"><use href="#i-caret"/></svg></button>
        <div class="mega" role="menu">
          <div class="mcol">
            <span class="lab">Train &amp; stay</span>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-route"/></svg></span><span class="mtx"><span class="mt">Stay &amp; Train Packages</span><span class="md">All-in week or month plans</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-clock"/></svg></span><span class="mtx"><span class="mt">Long-Stay Training</span><span class="md">Settle in for the season</span></span></a>
          </div>
          <div class="mcol">
            <span class="lab">Logistics</span>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-bed"/></svg></span><span class="mtx"><span class="mt">Where to Stay</span><span class="md">Partner accommodation nearby</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-scooter"/></svg></span><span class="mtx"><span class="mt">Scooter Rental</span><span class="md">Get around on two wheels</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-pin"/></svg></span><span class="mtx"><span class="mt">Getting Here</span><span class="md">Find &amp; reach the gym</span></span></a>
          </div>
          <div class="mcol">
            <span class="lab">Explore Chiang Mai</span>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-map"/></svg></span><span class="mtx"><span class="mt">Chiang Mai Guide</span><span class="md">Food, temples &amp; nature</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-leaf"/></svg></span><span class="mtx"><span class="mt">Things to Do</span><span class="md">Make the most of your stay</span></span></a>
          </div>
          <div class="mfeat">
            <span class="ft-tag">Make it home</span>
            <h4>Your Chiang Mai <span class="em">base.</span></h4>
            <p>Train daily, recover by the pool, find your people. Week or season — you belong here.</p>
            <div class="ff"><a class="fbtn" href="#">Plan your stay <ArrowWild class="fbtn-ar" size={20} /></a></div>
          </div>
          <div class="mfoot">
            <div class="links">
              <a href="#"><svg><use href="#i-id"/></svg> Staying long-term? See DTV Visa</a>
            </div>
            <a class="allcta" href="#">Stay &amp; Train overview <ArrowWild size={18} /></a>
          </div>
        </div>
      </div>

      <!-- DTV VISA -->
      <div class="mi wide" data-mega>
        <button aria-haspopup="true" aria-expanded="false"><span class="lbl">DTV Visa</span> <svg class="caret"><use href="#i-caret"/></svg></button>
        <div class="mega" role="menu">
          <div class="mcol">
            <span class="lab">The DTV guide</span>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-id"/></svg></span><span class="mtx"><span class="mt">What is the DTV Visa</span><span class="md">The 5-year soft-power visa, explained</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-check"/></svg></span><span class="mtx"><span class="mt">Am I Eligible?</span><span class="md">Requirements &amp; who qualifies</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-route"/></svg></span><span class="mtx"><span class="mt">How to Apply</span><span class="md">Step-by-step walkthrough</span></span></a>
          </div>
          <div class="mcol">
            <span class="lab">Train on a DTV</span>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-glove"/></svg></span><span class="mtx"><span class="mt">Muay Thai &amp; the DTV</span><span class="md">Training as your qualifying activity</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-clock"/></svg></span><span class="mtx"><span class="mt">Long-Stay Training Plans</span><span class="md">Programs built for the DTV</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-book"/></svg></span><span class="mtx"><span class="mt">Your Training Documents</span><span class="md">We provide your enrolment letter</span></span></a>
          </div>
          <div class="mcol">
            <span class="lab">Get help</span>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-help"/></svg></span><span class="mtx"><span class="mt">DTV FAQ</span><span class="md">Common questions answered</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-chat"/></svg></span><span class="mtx"><span class="mt">Talk to Us</span><span class="md">Ask about your situation</span></span></a>
          </div>
          <div class="mfeat gold">
            <span class="stamp"></span>
            <div class="dtv-mini"><b>DTV</b><i>Visa</i></div>
            <h4>Stay up to <span class="em">5 years.</span></h4>
            <p>Enrol long-term and we handle your training documents to support your application.</p>
            <div class="ff"><a class="fbtn" href="#">Start your DTV <ArrowWild class="fbtn-ar" size={20} /></a></div>
          </div>
          <div class="mfoot">
            <div class="links">
              <a href="#"><svg><use href="#i-scooter"/></svg> Scooter rental for your stay</a>
              <a href="#"><svg><use href="#i-bed"/></svg> Accommodation</a>
            </div>
            <a class="allcta" href="#">DTV Visa guide <ArrowWild size={18} /></a>
          </div>
        </div>
      </div>

      <!-- ABOUT -->
      <div class="mi" data-mega>
        <button aria-haspopup="true" aria-expanded="false"><span class="lbl">About</span> <svg class="caret"><use href="#i-caret"/></svg></button>
        <div class="mega mega--about" role="menu">
          <div class="mcol">
            <span class="lab">WildCat</span>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-leaf"/></svg></span><span class="mtx"><span class="mt">Our Story</span><span class="md">Family-led, born in Chiang Mai</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-user"/></svg></span><span class="mtx"><span class="mt">Meet the Coaches</span><span class="md">Real Thai trainers &amp; warmth</span></span></a>
          </div>
          <div class="mcol">
            <span class="lab">Community</span>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-users"/></svg></span><span class="mtx"><span class="mt">Our Community</span><span class="md">Members from around the world</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-camera"/></svg></span><span class="mtx"><span class="mt">Gallery</span><span class="md">Life at the gym</span></span></a>
            <a class="mlink" href="#"><span class="mic"><svg class="i"><use href="#i-star"/></svg></span><span class="mtx"><span class="mt">Reviews</span><span class="md">What members say</span></span></a>
          </div>
          <div class="mfeat">
            <span class="ft-tag">Why WildCat</span>
            <h4>Run by a woman, built like a <span class="em">family.</span></h4>
            <p>No ego at the door — just real training and real people.</p>
            <div class="ff"><a class="fbtn" href="#">Our story <ArrowWild class="fbtn-ar" size={20} /></a></div>
          </div>
        </div>
      </div>

      <!-- SIMPLE LINKS -->
      <div class="mi"><a href="#"><span class="lbl">Blog</span></a></div>
      <div class="mi"><a href="#"><span class="lbl">Contact</span></a></div>
      <div class="mi cta"><Button variant="gold" flat arrow href="#">{ctaLabel}</Button></div>
    </nav>

    <button class="nav-toggle" aria-label="Open menu" data-drawer-open>
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
    </button>
  </div>
</header>

<!-- ============ MOBILE DRAWER ============ -->
<div class="drawer" id="wc-drawer">
  <div class="scrim" data-drawer-close></div>
  <div class="panel">
    <div class="dh"><img src="/assets/logo-cream.png" alt="WildCat"><button class="x" aria-label="Close" data-drawer-close>×</button></div>
    <div class="dbody">
      <details open><summary>Train <span class="pm">+</span></summary><div class="sub-links"><a href="#"><span class="dot"></span> Beginner Muay Thai</a><a href="#"><span class="dot"></span> All Levels</a><a href="#"><span class="dot"></span> Private Sessions</a><a href="#"><span class="dot"></span> Women's Muay Thai</a><a href="#"><span class="dot"></span> Kids &amp; Family</a><a href="#"><span class="dot"></span> Fight Team</a><a href="#"><span class="dot"></span> Schedule &amp; Pricing</a></div></details>
      <details><summary>Stay &amp; Train <span class="pm">+</span></summary><div class="sub-links"><a href="#"><span class="dot"></span> Packages</a><a href="#"><span class="dot"></span> Accommodation</a><a href="#"><span class="dot"></span> Scooter Rental</a><a href="#"><span class="dot"></span> Getting Here</a><a href="#"><span class="dot"></span> Chiang Mai Guide</a></div></details>
      <details><summary>DTV Visa <span class="pm">+</span></summary><div class="sub-links"><a href="#"><span class="dot"></span> DTV Visa Guide</a><a href="#"><span class="dot"></span> Am I Eligible?</a><a href="#"><span class="dot"></span> How to Apply</a><a href="#"><span class="dot"></span> Muay Thai &amp; the DTV</a><a href="#"><span class="dot"></span> DTV FAQ</a></div></details>
      <details><summary>About <span class="pm">+</span></summary><div class="sub-links"><a href="#"><span class="dot"></span> Our Story</a><a href="#"><span class="dot"></span> Coaches</a><a href="#"><span class="dot"></span> Community</a><a href="#"><span class="dot"></span> Gallery &amp; Reviews</a></div></details>
      <a class="flat" href="#">Blog</a>
      <a class="flat" href="#">Contact</a>
      <Button variant="gold" flat arrow href="#" class="djoin">{ctaLabel}</Button>
    </div>
  </div>
</div>

<style>
  .util {
    background: #21422c;
    color: rgba(246, 241, 230, 0.72);
    font-size: 12.5px;
  }
  .util .wrap {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 38px;
    gap: 18px;
  }
  .wrap {
    max-width: var(--maxw);
    margin: 0 auto;
    padding: 0 clamp(18px, 3vw, 32px);
  }
  .util .u-left { display: flex; align-items: center; gap: 18px; }
  .util .u-item { display: flex; align-items: center; gap: 7px; white-space: nowrap; }
  .util .u-item svg { width: 14px; height: 14px; color: var(--gold); }
  .util .dot-open { width: 7px; height: 7px; border-radius: 50%; background: #5bb487; box-shadow: 0 0 0 3px rgba(91, 180, 135, 0.2); }
  .util .u-right { display: flex; align-items: center; gap: 16px; }
  .util .u-soc { display: flex; gap: 11px; }
  .util .u-soc a { display: flex; opacity: 0.8; transition: 0.15s; }
  .util .u-soc a:hover { opacity: 1; color: var(--gold); }
  .util .u-soc svg { width: 15px; height: 15px; }
  .util .lang b { color: var(--gold); }
  .util .lang span { opacity: 0.5; }

  /* MAIN NAV */
  .nav {
    position: sticky;
    top: 0;
    z-index: 80;
    background: var(--forest);
    color: var(--cream);
    border-bottom: 1px solid rgba(246, 241, 230, 0.1);
    font-family: var(--font-display);
  }
  .nav--static { position: relative; }
  .nav .wrap { display: flex; align-items: center; justify-content: space-between; gap: 18px; height: 74px; }
  .brand img { height: 44px; width: auto; display: block; }
  .menubar { display: flex; align-items: stretch; height: 74px; gap: 2px; }
  .mi { position: relative; display: flex; align-items: center; }
  .mi > a,
  .mi > button {
    font-family: inherit;
    background: none;
    border: 0;
    cursor: pointer;
    color: rgba(246, 241, 230, 0.85);
    font-weight: 600;
    font-size: 14.5px;
    height: 100%;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 0 15px;
    position: relative;
  }
  .mi > a:hover,
  .mi > button:hover,
  .mi.open > button { color: var(--cream); }
  .mi > button .caret { width: 11px; height: 11px; transition: transform 0.22s ease; opacity: 0.7; }
  .mi.open > button .caret { transform: rotate(180deg); }
  /* gold underline indicator — sous le texte (pas au bas de la navbar) */
  .lbl { position: relative; display: inline-block; }
  .lbl::after {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    bottom: -7px;
    height: 2px;
    background: var(--gold);
    border-radius: 2px;
    transform: scaleX(0);
    transform-origin: center;
    transition: transform 0.22s ease;
  }
  .mi:hover > a .lbl::after,
  .mi:hover > button .lbl::after,
  .mi.open > button .lbl::after { transform: scaleX(1); }
  .mi.cta { margin-left: 10px; }
  .nav-toggle { display: none; background: none; border: 0; color: var(--cream); cursor: pointer; }
  .nav-toggle svg { width: 28px; height: 28px; }
  svg.i { width: 20px; height: 20px; flex: 0 0 auto; }
  .caret { display: inline-block; }

  /* MEGA PANEL */
  .mega {
    --shift: 0px;
    position: absolute;
    top: calc(100% + 10px);
    left: 50%;
    transform: translate(calc(-50% + var(--shift)), 8px);
    width: min(1100px, calc(100vw - 24px));
    background: var(--surface);
    color: var(--ink);
    border: 1px solid var(--line);
    border-radius: var(--r-lg);
    box-shadow: 0 30px 70px -34px rgba(20, 35, 25, 0.55);
    padding: 26px;
    display: grid;
    grid-template-columns: 1fr 1fr 1fr 0.92fr;
    gap: 8px 20px;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: opacity 0.2s ease, transform 0.2s ease;
    z-index: 90;
  }
  /* bridge transparent pour combler le gap navbar↔panel au survol */
  .mega::before {
    content: '';
    position: absolute;
    top: -12px;
    left: 0;
    right: 0;
    height: 12px;
  }
  .mega--about { grid-template-columns: 1fr 1fr 0.95fr; width: min(720px, calc(100vw - 24px)); }
  .mi.open .mega { opacity: 1; visibility: visible; pointer-events: auto; transform: translate(calc(-50% + var(--shift)), 0); }

  .mcol { display: flex; flex-direction: column; }
  .mcol .lab {
    font-size: 11px; font-weight: 800; letter-spacing: 0.14em; text-transform: uppercase;
    color: var(--gold-deep); margin: 2px 0 12px; display: flex; align-items: center; gap: 7px;
  }
  .mcol .lab::before { content: '◆'; color: var(--orange); font-size: 9px; }
  .mlink { display: flex; gap: 11px; align-items: flex-start; padding: 9px 10px; border-radius: var(--r-sm); transition: 0.14s; margin: 0 -10px; }
  .mlink:hover { background: var(--soft); }
  .mlink .mic { width: 34px; height: 34px; flex: 0 0 auto; border-radius: 9px; background: var(--soft-2); color: var(--forest); display: flex; align-items: center; justify-content: center; transition: 0.14s; }
  .mlink:hover .mic { background: var(--forest); color: var(--gold); }
  .mlink .mtx { min-width: 0; }
  .mlink .mt { display: block; font-weight: 700; font-size: 14.5px; color: var(--forest); line-height: 1.25; white-space: nowrap; }
  .mlink .md { display: block; font-size: 12.5px; color: var(--ink-2); line-height: 1.35; margin-top: 2px; }

  /* featured promo card */
  .mfeat { background: var(--forest); color: var(--cream); border-radius: var(--r-md); padding: 20px; display: flex; flex-direction: column; position: relative; overflow: hidden; }
  .mfeat.gold { background: radial-gradient(120% 90% at 80% 0, #356a44, #2e5d3c); }
  .mfeat .ft-tag { font-size: 10.5px; font-weight: 800; letter-spacing: 0.12em; text-transform: uppercase; color: var(--gold); margin-bottom: 10px; }
  .mfeat h4 { font-family: var(--font-display); font-weight: 800; font-size: 19px; line-height: 1.1; letter-spacing: -0.01em; margin: 0 0 7px; }
  .mfeat h4 .em { color: var(--gold); }
  .mfeat p { font-size: 12.5px; color: rgba(246, 241, 230, 0.74); line-height: 1.45; margin: 0 0 16px; }
  .mfeat .ff { margin-top: auto; }
  .fbtn { display: inline-flex; align-items: center; gap: 8px; background: var(--gold); color: var(--charcoal); font-family: var(--font-display); font-weight: 700; font-size: 13px; padding: 9px 16px; border-radius: 999px; transition: 0.16s; }
  .fbtn:hover { background: #e8b13e; }
  .fbtn-ar { transition: transform 0.18s ease; }
  .fbtn:hover .fbtn-ar { transform: translateX(3px); }
  .mfeat .stamp { position: absolute; right: -26px; top: -26px; width: 120px; height: 120px; border-radius: 50%; background: radial-gradient(circle at 50% 38%, #efb63a, #db9a14); border: 2px solid #1c1407; opacity: 0.16; }
  .dtv-mini { width: 62px; height: 62px; border-radius: 50%; background: radial-gradient(circle at 50% 36%, #efb63a, #db9a14); border: 2px solid #1c1407; color: #1c1407; display: flex; flex-direction: column; align-items: center; justify-content: center; font-weight: 800; line-height: 0.85; margin-bottom: 14px; position: relative; }
  .dtv-mini b { font-size: 21px; }
  .dtv-mini i { font-style: normal; font-size: 7px; letter-spacing: 0.1em; text-transform: uppercase; margin-top: 3px; }

  /* mega footer strip */
  .mega .mfoot { grid-column: 1 / -1; margin-top: 6px; padding-top: 14px; border-top: 1px solid var(--line); display: flex; align-items: center; justify-content: space-between; gap: 14px; flex-wrap: wrap; }
  .mega .mfoot .links { display: flex; gap: 18px; flex-wrap: wrap; }
  .mega .mfoot a { font-size: 12.5px; font-weight: 600; color: var(--ink-2); display: inline-flex; align-items: center; gap: 6px; }
  .mega .mfoot a:hover { color: var(--forest); }
  .mega .mfoot .links a svg { width: 14px; height: 14px; color: var(--gold-deep); }
  .mega .mfoot .allcta { font-size: 12.5px; font-weight: 700; color: var(--gold-deep); display: inline-flex; align-items: center; gap: 6px; }

  /* RESPONSIVE */
  @media (max-width: 1080px) {
    .menubar { display: none; }
    .nav-toggle { display: block; }
  }

  /* MOBILE DRAWER */
  .drawer { display: none; position: fixed; inset: 0; z-index: 200; }
  .drawer.open { display: block; }
  .drawer .scrim { position: absolute; inset: 0; background: rgba(17, 17, 17, 0.55); }
  .drawer .panel { position: absolute; top: 0; right: 0; bottom: 0; width: min(86vw, 360px); background: var(--cream); overflow-y: auto; box-shadow: 0 30px 70px -34px rgba(20, 35, 25, 0.55); font-family: var(--font-display); }
  .drawer .dh { position: sticky; top: 0; background: var(--forest); display: flex; align-items: center; justify-content: space-between; padding: 16px; }
  .drawer .dh img { height: 32px; }
  .drawer .dh .x { background: none; border: 0; color: var(--cream); font-size: 28px; cursor: pointer; line-height: 1; }
  .drawer .dbody { padding: 8px 14px 30px; }
  .drawer details { border-bottom: 1px solid var(--line); }
  .drawer summary { list-style: none; cursor: pointer; padding: 15px 6px; display: flex; align-items: center; justify-content: space-between; font-weight: 700; font-size: 16px; color: var(--forest); }
  .drawer summary::-webkit-details-marker { display: none; }
  .drawer summary .pm { color: var(--gold-deep); font-size: 20px; transition: 0.2s; }
  .drawer details[open] summary .pm { transform: rotate(45deg); }
  .drawer .sub-links { padding: 0 6px 12px; display: flex; flex-direction: column; gap: 1px; }
  .drawer .sub-links a { padding: 10px 12px; font-size: 14px; font-weight: 600; color: var(--ink-2); border-radius: 7px; display: flex; align-items: center; gap: 9px; }
  .drawer .sub-links a:hover { background: var(--soft); color: var(--forest); }
  .drawer .sub-links a .dot { width: 5px; height: 5px; border-radius: 50%; background: var(--gold); }
  .drawer .flat { padding: 15px 6px; font-weight: 700; font-size: 16px; color: var(--forest); border-bottom: 1px solid var(--line); display: block; }
  .drawer :global(.djoin) { margin: 18px 6px 0; width: calc(100% - 12px); justify-content: center; }
</style>

<script>
  (function () {
    const items = Array.from(document.querySelectorAll('.mi[data-mega]'));
    let closeTimer: ReturnType<typeof setTimeout> | null = null;

    function closeAll(except?: Element | null) {
      items.forEach((it) => {
        if (it !== except) {
          it.classList.remove('open');
          it.querySelector('button')?.setAttribute('aria-expanded', 'false');
        }
      });
    }
    function clamp(it: Element) {
      const p = it.querySelector('.mega') as HTMLElement | null;
      if (!p) return;
      p.style.setProperty('--shift', '0px');
      const r = p.getBoundingClientRect();
      const m = 12;
      let shift = 0;
      if (r.right > window.innerWidth - m) shift -= r.right - (window.innerWidth - m);
      if (r.left + shift < m) shift += m - (r.left + shift);
      if (shift) p.style.setProperty('--shift', Math.round(shift) + 'px');
    }
    function open(it: Element) {
      if (closeTimer) clearTimeout(closeTimer);
      closeAll(it);
      it.classList.add('open');
      it.querySelector('button')?.setAttribute('aria-expanded', 'true');
      clamp(it);
    }
    function close(it: Element) {
      it.classList.remove('open');
      it.querySelector('button')?.setAttribute('aria-expanded', 'false');
    }

    items.forEach((it) => {
      const btn = it.querySelector('button');
      it.addEventListener('mouseenter', () => open(it));
      it.addEventListener('mouseleave', () => {
        closeTimer = setTimeout(() => close(it), 120);
      });
      btn?.addEventListener('click', (e) => {
        e.preventDefault();
        it.classList.contains('open') ? close(it) : open(it);
      });
    });
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') closeAll(null);
    });
    document.addEventListener('click', (e) => {
      if (!(e.target as Element)?.closest('.menubar')) closeAll(null);
    });

    // mobile drawer
    const drawer = document.getElementById('wc-drawer');
    document.querySelectorAll('[data-drawer-open]').forEach((b) =>
      b.addEventListener('click', () => drawer?.classList.add('open'))
    );
    document.querySelectorAll('[data-drawer-close]').forEach((b) =>
      b.addEventListener('click', () => drawer?.classList.remove('open'))
    );
  })();
</script>
