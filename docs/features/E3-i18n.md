# E3 — i18n (multi-langue)

**Complexité : M · Statut : DONE (2026-06-03)**

## Description
Routing localisé, gestion des traductions, hreflang et lang switcher. V1 = EN/FR (maquette). Extension TH/DE/ES/RU = V1.1.

## Tâches
- [x] Config Astro i18n (`defaultLocale: en`, locales, `routing`) — fait en E1
- [x] Intégrer Paraglide JS (`@inlang/paraglide-js` 2.x, plugin Vite) + messages `messages/en.json` / `fr.json` (144 clés, parité vérifiée)
- [x] `[lang]/` routing + redirection racine `/` → `/en` (statique ; détection `Accept-Language` = amélioration possible côté edge en E10)
- [x] `LangSwitcher` (utility bar, conforme maquette) — préserve le path courant, `aria-current` + `hreflang`, zéro JS
- [x] `hreflang` automatique par page (alternates BaseLayout, posé en E2, vérifié)
- [x] Messages typés `m.*()`, **zéro string UI en dur** (Nav, Footer, BaseLayout, home, FileUpload ; styleguide interne exclu)
- [x] Décision slugs (Q6) : hors scope E3 — réglée de facto par Sanity document-level i18n (E4/E5)

## Décisions techniques
- Astro i18n natif (routing) + Paraglide 2.x (messages) — détail et pièges dans `docs/DECISIONS.md` (entrée 2026-06-03 i18n).
- SSG : locale posée par `src/middleware.ts` (`setLocale`), strategy `['url','globalVariable','baseLocale']`.
- Source : `messages/*.json` (versionné) ; `src/paraglide/` compilé (gitignored).
- DeepL pour pré-traduire les langues secondaires en V1.1 (review humaine ensuite).

## Notes / edge cases
- Clé FR manquante = fallback EN **silencieux** : vérifier la parité des sets de clés EN/FR à chaque ajout (build vert ≠ FR complet).
- Pas d'accès dynamique `m['…']` (tree-shaking/typage) → records explicites (cf. Footer, Nav ctaLabels).
- Reporté E5 : `pricing[].label` de `site.ts` (donnée de page Pricing).
- En dur volontairement : marque "Wildcat" (`site.name`), badge graphique DTV/Visa, noms propres sociaux (Instagram/Facebook).
