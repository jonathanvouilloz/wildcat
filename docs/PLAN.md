# PLAN — Wildcat Muay Thai

Plan d'exécution maître. Statuts : `TODO` · `EN COURS` · `DONE` · `BLOQUÉ`.

---

## Vue d'ensemble des epics

| # | Epic | Complexité | Statut | Détail |
|---|------|:---:|:---:|--------|
| E1 | Foundation & Setup | M | **DONE** | historique : `DECISIONS.md` + entrées ci-dessous |
| — | Design system (typo, boutons, nav/mega menu, icônes Tabler) | M | **DONE** | `docs/DESIGN.md` + `/styleguide` |
| E2 | App shell & design-system completion | M | **DONE** | historique : `DECISIONS.md` + entrées ci-dessous |
| E3 | i18n (EN/FR) | M | **DONE** | historique : `DECISIONS.md` + entrées ci-dessous |
| E4 | Sanity CMS | M | **DONE** | historique : `DECISIONS.md` + entrées ci-dessous |
| E5 | Pages cœur & silos SEO | L | **DONE** (E5 + E5b→f) | historique : `DECISIONS.md` + entrées ci-dessous |
| E6 | Mini-app DTV | L | TODO | [E6-dtv-app.md](features/E6-dtv-app.md) |
| E7 | SEO technique | M | TODO | [E7-seo-technical.md](features/E7-seo-technical.md) |
| E8 | Blog & contenu | M | TODO | [E8-blog-content.md](features/E8-blog-content.md) |
| E9 | Médias & galerie | S | TODO | [E9-media.md](features/E9-media.md) |
| E10 | Déploiement Vercel | S | TODO | [E10-deploy.md](features/E10-deploy.md) |

> `docs/features/` ne contient que les epics **restants** (E6→E10) — les specs des epics livrés sont supprimées, leur historique vit dans `DECISIONS.md` et les entrées datées ci-dessous.

---

## Ordre d'exécution proposé

```
E1 Foundation
  └─> E2 Design System ──┐
  └─> E3 i18n            ├─> E5 Pages cœur ──> E7 SEO technique ──> E8 Blog ──> E9 Médias ──> E10 Deploy
  └─> E4 Sanity ─────────┘                └─> E6 DTV app
```

1. **E1 Foundation** — scaffold Astro/Tailwind, tokens, fonts, structure. (socle de tout)
2. **E2 / E3 / E4 en parallèle après E1** — layout+nav, i18n, schémas Sanity.
3. **E5 Pages cœur** — Home → DTV pillar → Stay & Train → Classes → About → Contact.
4. **E6 DTV app** — peut démarrer dès que E5 (page pillar) + storage (Q1) tranchés.
5. **E7 SEO technique** — sitemap, hreflang, schema.org, OG (une fois les pages en place).
6. **E8 Blog** + **E9 Médias** — contenu et galerie.
7. **E10 Deploy** — Vercel + domaine + env de prod.

## Prochaines étapes prioritaires (reprise session suivante)

1. ✅ **E1–E4 faits** : scaffold + design system + app shell + i18n Paraglide + Sanity (studio `/studio`, projet `etmrqgb2`). Détails : entrées précédentes + `docs/DECISIONS.md`.
2. ✅ **E5 (pages prioritaires) fait** — 7 pages EN+FR : **Home** (port fidèle de la maquette landing sur le thème), **/dtv-visa** (pillar + FAQPage JSON-LD), **/stay-train**, **/classes** (#schedule #pricing — prix publics complets), **/fighters** (CMS-only, coming soon si vide), **/about** (#coaches), **/contact** (+ **`/api/contact`** : serverless, fetch→Resend gardé par `RESEND_API_KEY`). ~20 composants sections (Hero, PageHero, ValueStrip, DtvTeaser/Steps/Stamp, Program/Coach/Fighter/Pricing cards+grids, ScheduleTable, TestimonialQuote, Faq, CtaBanner, FeatureGrid, Breadcrumb, ContactForm…). **`safeQuery`** + fallbacks messages → build vert dataset vide. Nav/Footer re-pointés (clusters → pillar+anchor, zéro 404 ; Blog `#` jusqu'à E8). 466 clés messages, parité EN/FR vérifiée. JSON-LD : SportsClub (home), FAQPage (dtv), BreadcrumbList (pages intérieures).
3. ✅ **Satellites DTV (E5b, 2026-06-04, branche `feat/dtv-satellites`)** — 5 pages EN+FR data-driven (DataForSEO + SERP scans + briefs YMYL dans `content/_drafts/pages/`) : eligibility (+quiz), how-to-apply (+checklist docs, HowTo JSON-LD), muay-thai (table DTV vs ED, kw diff 0), long-stay-training (+estimateur budget), faq (30 Q, FAQPage JSON-LD déplacé du pillar). 4 composants interactifs vanilla (`src/components/interactive/`), Nav/pillar remaillés, +563 clés (1029, parité OK), tests Playwright 26/26. Contexte SEO projet créé : `docs/identity.md`, `docs/voice.md`, `docs/seo-context.md` (auteur contenus : Meaw Boonpradub).
4. ✅ **Satellites Train (E5c, 2026-06-04, branche `feat/train-cluster`)** — architecture data-driven 9 pages → 2 (`docs/structure-train.md`, recherche DataForSEO US/UK/FR + 2 scans SERP) : **`/classes/beginners`** créé (kw "beginner muay thai classes" 1000/mo diff 0, variante A, byline Meaw + Article JSON-LD, walkthrough 5 étapes, **FirstClassChecklist** = réutilisation `DocChecklist` via prop `storageKey`) + **re-cadrage pillar `/classes`** (7 modifs : title/meta/H1 variante A, cards enrichies, section `#camp` dark "open-air family camp", transparence pricing 300-700 THB + liens long-stay, FAQ → 5 PAA SERP). Nav "Beginner Muay Thai" → satellite (seul changement menu). Maillage : home, /stay-train, /dtv-visa/eligibility, pillar (card + FAQ). +146 clés (1165, parité OK), `/seo-review` ×2 PASS, Playwright **51/51**. Insight FR "camp/stage muay thai thailande" (210+140/mo) → backlog Stay & Train batch 2 (`structure-train.md` §5). Faits vérifiés (Jonathan, 2026-06-04) : ✅ adresse Nong Kwai/Hang Dong (copy camp ancrée + map embed `/contact`) · ✅ gants+bandes prêtés · ⏳ âge mini kids (~6 ans estimé, à valider — non publié).
5. ✅ **Fighters livre d'or (E5d, 2026-06-04/05, branche `feat/train-cluster`)** — re-cadrage `/fighters` en **guest book** (guests ayant combattu à Chiang Mai grâce à la famille) : **pas de record W/L** (stat unique = combats pris, victoires = stamps), pas de status current/alumni. Schéma migré (−status/wins/losses/bio/nationality ; +fightName, countryCode, year, venues ×4, quote localeString, **achievements ×10 en checkboxes**). Design itéré sur `/styleguide` (protos polaroid F·A-D → worn photo W·A-D) jusqu'au final **« incrusté scrapbook » v2** : photo posée sur le fond, bord déchiré seedé (2 frames carrés × 4 rotations × 2 miroirs CSS = 16 variantes, photo contre-tournée), scotch seedé (3 assets), **10 stamps encre die-cut** (générés par Jonathan) en grappe côté opposé au scotch (max 3), ornement nom + drapeau SVG `flag-icons` inliné au build, quote manuscrite **Caveat** (`--font-hand`). Pipelines rejouables : `tests/process-stamp.py` (die-cut) + `tests/process-frame.py` (SVG cadre → mask). Compteur agrégé (dès 3 membres), 1179 clés (parité OK), **vérifié en réel** (1er fighter saisi au studio).
6. ✅ **Silo About (E5e, 2026-06-04, branche `feat/train-cluster`)** — fin des liens menu "tout vers /about" : **`/about/coaches`** créé (page équipe + **entité auteur E-E-A-T** : bloc Meaw message-driven, Person JSON-LD `#meaw`, CoachGrid, 3 valeurs) ; `/about` re-travaillé (coaches → teaser `#coaches` conservé, `#community`, **nouvelle section `#reviews`** : grille testimonials + CTA avis Google `site.reviews.googleUrl` TODO) ; **PageHero variante photo** (prop `image`+`eyebrow`, direction B compacte) sur les 2 pages ; mega menu About re-pointé + **Gallery retirée jusqu'à E9** ; maillage : `author.url` (Article+HowTo JSON-LD) + bylines cliquables ×5 satellites + footer → `/about/coaches`. **1223 clés (parité OK)**, build vert, Playwright 51/51. ⏳ portrait Meaw + lien direct avis Google.
7. ✅ **Session 2026-06-05** : **hero home tranché** (direction actuelle conservée, photo upgradée + recadrage center 60%) · **Q5 domaine tranché** : `wildcatmuaythai.com` (+ `wildcatchiangmai.com` 301 défensif, dispo RDAP OK — **achat à faire**) · **fact-check DTV** (sub-agent vs sources officielles → `docs/dtv-fact-check.md` : 27/30 confirmés, 1 erreur corrigée — âge 18+→20 ans `dtv_faqp_a8` EN/FR ; reste : PDF checklist MFA à valider avec Meaw) · **feed Instagram réel** : fetch build-time via **Apify** (`src/lib/instagram.ts`, ~0,006 $/build, plan gratuit) + composant réutilisable **`InstagramFeed`** (images CDN signées re-hébergées en local par `astro:assets`, fallback placeholders si token absent, dev désactivé sauf `INSTAGRAM_DEV_FETCH=1`) branché home + `/about#community` — rafraîchissement = chaque deploy (cron deploy hook tous les 3-4 j à poser en E10). 1225 clés (parité OK).
8. ✅ **Silo Stay & Train (E5f, 2026-06-05, branche `feat/train-cluster`)** — pipeline data-driven complet (`docs/structure-stay.md`) : recherche DataForSEO ×7 (US+FR) → **"muay thai camp thailand" 1900/mo diff 29 LOW découvert** (la page visait un kw ~10/mo) + 5 satellites maquette invalidés (accommodation 0, holiday 10, stay-and-train 0, scooter hors silo, séjour FR 0) ; scans SERP ×3 (gaps 8/10 : famille + prix THB transparents + DTV absents de toutes les pages 1 ; FR = listicles sans réservation + agences à marge). **Verdict : 0 satellite, re-cadrage majeur du pillar `/stay-train`** (brief `stay-pillar.md`, variante B "Packages & Prices") : EN tête nationale + FR ré-anglé "camp/stage" (H1 camp, stage en H2, angle réservation directe sans marge — claim "accueil en français" abandonné, équipe anglophone) ; sections **#packages** (table inclus/à organiser), **#accommodation** (condos de Meaw — Q-A PRD tranchée), **#life**, **FAQ 6 PAA + FAQPage JSON-LD** (porteur unique silo) ; `Faq` étendu (prop `link`) ; Nav re-pointé (6 mega + drawer, Long-Stay → `/dtv-visa/long-stay-training`) ; maillage 6 cibles. **1236 clés (parité OK)**, audit SEO EN+FR vert, **Playwright 69/69** (+18 tests Stay, `WC_BASE` paramétrable). ⏳ avant prod : détail packages + condos avec Meaw, photos #accommodation/#life.
9. ✅ **Cards programme & pricing (2026-06-05, branche `feat/train-cluster`)** — explorations 4 directions sur `/styleguide` (relief brutalist, ticket fight night, poster sombre, scrapbook) itérées avec Jonathan → **`ProgramCard` « incrustée scratch »** : zéro ombre/bordure, carte entière masquée par les frames déchirées du guest book (2 assets × 2 miroirs, contre-flip du contenu, seed sur le titre), transition déchirée photo→carte, niveau manuscrit Caveat, ArrowWild, pas d'effet hover — fonds cream/soft uniquement ; **`PricingCard` « scrapbook »** : coches Caveat, prix cerclé au marqueur (populaire), tampon `decor/stamp-most-popular.webp` (asset Jonathan, pipeline die-cut, rendu opaque), aucune bordure colorée. **API inchangées → zéro modif pages** (home/classes/stay-train stylées d'office). Styleguide : protos remplacés par la démo des composants réels, vieux mocks retirés. Build ✅, Playwright 67/69 (2 FAIL = tests Stay FR du re-cadrage en attente d'intégration).
10. ✅ **Satellite Scooter Rental + checklist Meaw (2026-06-05, branche `feat/train-cluster`)** — **`docs/checklist.md`** créé (inventaire exhaustif de tout ce qui dépend de Meaw : DTV §A, packages §B, condos §C, scooters §D, cours §E, admin §F, studio §G, photos §H, divers §I + top 10). **Verdict scooter E5f renversé** (Meaw loue réellement : Scoopy i Prestige 2024 350/j 3000/mois + Click) → **`/stay-train/scooter-rental`** créé (kw "scooter rental chiang mai" 210/mo diff 24, angle loueur au camp Hang Dong) : **collection Sanity `scooter`** (toggle `available` au studio, prix THB, photo) + fallback `site.scooterFleet`, fleet cards (badge dispo, "ask us" si prix manquant — Click), how-it-works prudent (conditions exactes → checklist §D), FeatureGrid why, FAQ visible sans FAQPage (porteur = pillar), **Service JSON-LD** (offers 350/3000 THB). Nav re-pointée ×3 (mega, drawer, foot DTV) + maillage stay-train#life, long-stay, packages. **1295 clés (parité OK)**, audit SEO EN+FR PASS (metas raccourcies 147/144, H1 + Chiang Mai), build ✅.
11. ✅ **Système d'intake contact (2026-06-05, branche `feat/train-cluster`)** — refonte conversion à 3 étages : **`ContactForm` v2 intent-driven** (5 intentions en radio-cards, champs contextuels togglés en pur CSS `:has()` no-JS, présélection `?intent=` + legacy `?topic=`, fast-lane WhatsApp pré-rempli par intention) · **`src/lib/whatsapp.ts`** (`waLink` — textes wa.me EN dans les 2 locales, équipe anglophone) · **`/api/contact` v2** (champs whitelistés par intention → email pré-qualifié, redirect `from` whitelisté) · **`StayInquiryForm` inline `#plan`** sur `/stay-train` (la conversion séjour reste sur la page) · **scooter WhatsApp-first** (wa pré-rempli par modèle sur les fleet cards) · **sweep ~40 CTAs** (`?intent=class/dtv/scooter`, cards programmes 2-5 en « Book this class », Nav `ctaHref` contextuel). **1370 clés (parité OK)**, build ✅.
12. ▶️ **Reprise — ce qu'il manque (état au 2026-06-05 soir)** :
   - **Avec Meaw — tout est consolidé dans `docs/checklist.md`** (faits, non bloquants — copy écrite pour ne pas en dépendre) : détail exact "what's included" des packages week/month/fighter (`structure-stay.md` §0 9b) · condos (prix, nombre, distance du camp) · **scooters (nombre total, prix Click, caution/casques/permis/essence — §D)** · âge mini kids (~6 ans estimé) · PDF checklist MFA (fact-check DTV)
   - **Photos à fournir (Jonathan)** : `#accommodation` (condo/chambre) + `#life` (entre deux sessions) sur `/stay-train` — sections livrées en texte seul (`structure-stay.md` §0 9c) · portrait Meaw (hero-team.webp en attendant)
   - **Contenu studio Sanity** : coaches, schedule, testimonials, fighters du livre d'or (le testimonial `/stay-train` n'apparaît que si saisi)
   - **Admin** : acheter `wildcatmuaythai.com` + `wildcatchiangmai.com` (Q5) · env prod : `APIFY_TOKEN` (+ ~~`RESEND_API_KEY`/`CONTACT_EMAIL`/`CONTACT_FROM`~~ caducs si migration Web3Forms actée — voir Dev) · email réel dans `site.ts` (`TODO(real data)`) · lien avis Google (`site.reviews.googleUrl`)
   - **Dev** : **E6 DTV app** (trancher **Q1** storage avant : Google Drive API vs Supabase Storage) · corriger les faits DTV selon `docs/dtv-fact-check.md` · redirect `/muay-thai-training` → `/stay-train` (E7/E10) · cron deploy hook Instagram tous les 3-4 j (E10) · **forms → Web3Forms** (décision 2026-06-05, remplace Resend ; POST direct `api.web3forms.com/submit`, supprimer `/api/contact` = site 100 % statique ; à ré-implémenter côté form : filtrage champs par intention via JS disable au submit, sujet pré-qualifié via `subject`, honeypot `botcheck` — détail dans CLAUDE.md)
   - **Blog E8 — calendrier éditorial tranché (2026-06-05) : `docs/structure-blog.md`** — rapport Kimi analysé + validé par DataForSEO (16 clusters de vérif, 575 kws ≥10/mo agrégés) → 6 mois × 6 clusters × 24 articles (M1 choisir-son-camp, M2 beginners, M3 benefits/transformation, M4 Chiang Mai life ⚠️ burning season en ligne avant décembre, M5 visa, M6 culture/comparaisons) + réserve. Règles : 10/mo = article OK (longue traîne), 0/mo → alternative comparaison, zéro cannibalisation pillars. Invalidés : bootcamp/coaching privé (hors offre), lead magnets PDF (hors V1), silo digital nomad (le volume = DTV déjà couvert)
   - **Backlog SEO** : re-vérifier le ranking `/stay-train` post-deploy (kw 1900/mo) · niche FR "camp muay thai thailande français" servie par le contenu — suivre en GSC
11. Données réelles manquantes : email (`src/config/site.ts`) — `TODO(real data)` ; ~~horaires~~ (✅ planning réel du flyer committé 2026-06-05, `site.schedule` + DaySchedule) ; détail packages séjour + condos Meaw (`structure-stay.md` §0 9b/9c). Redirect `/muay-thai-training` → `/stay-train` (E7/E10). ~~Pillar FR : title/meta à raccourcir~~ (réglé par le re-cadrage E5c — title 50ch / meta 143ch).

## Build-as-you-go restant (à créer avec leur page)
DTVStepper (E6), TestimonialSlider (si besoin réel — quote statique en E5), liste/article blog + `.prose` (E8), galerie + HeroVideo (E9), composants motifs additionnels (BrushDivider standalone/stripes).

## Pages prioritaires lancement (rappel PRD)

Home · DTV Visa (pillar) · DTV Apply (formulaire) · Stay & Train · Classes · About · Contact.
