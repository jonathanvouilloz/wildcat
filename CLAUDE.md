# CLAUDE.md — Wildcat Muay Thai Chiang Mai

## Résumé du projet

Site vitrine SEO multi-langue (Astro 6 + Sanity CMS) pour **Wildcat Muay Thai**, un camp de Muay Thai familial et open-air à Chiang Mai. Objectif : capter des clients étrangers via le SEO global/local et professionnaliser la gestion des visas DTV grâce à une mini-app de dépôt de dossier intégrée (formulaire multi-étapes + upload documents + WhatsApp + email). Positionnement de marque : chaleureux, accueillant, familial — **pas** un fight camp hardcore.

## Stack technique

| Couche | Choix |
|--------|-------|
| Framework | Astro 6 (latest stable ; SSG + routes on-demand, i18n natif) |
| Styling | Tailwind CSS v4 |
| CMS | Sanity v3 (headless) |
| i18n | Astro i18n natif (routing) + Paraglide JS 2.x (messages typés) — EN/FR en V1 |
| Email | Resend |
| Storage DTV | À valider — Google Drive API **ou** Supabase Storage |
| Comm DTV | WhatsApp deep link (`wa.me`) |
| Traduction auto | DeepL API (pour langues secondaires éventuelles) |
| Hosting | Vercel |

## Commandes utiles

```bash
npm install            # dépendances
npm run dev            # serveur dev (localhost:4321) — studio sur /studio
npm run build          # build production (→ dist/ + .vercel/output)
npm run preview        # preview du build
npm run sanity:types   # extract schéma + typegen → src/lib/sanity.types.ts (100% local)
```

## Workflow dev — serveur & tests

- **Un seul serveur dev, déjà actif** : Jonathan lance `npm run dev` (**http://localhost:4321**) en permanence à l'ouverture du projet. **Ne jamais lancer un nouveau serveur** (`npm run dev`, `npm run preview`, serveur statique…) — même avec plusieurs sessions/features en parallèle, toujours se baser sur ce serveur live. Pour les tests Playwright, le cibler via `WC_BASE=http://localhost:4321`.
- **Pas de tests spontanés** : ne pas lancer Playwright, screenshots ou autres vérifications navigateur sans demande explicite. En cas de doute, demander d'abord : « Tu veux que je teste, ou tu vérifies toi-même sur ton serveur ? ». Exception : `npm run build` reste OK pour valider que ça compile.
- Priorité au code et à la fonctionnalité — la vérification visuelle, c'est Jonathan qui la fait sur sa session live.

## Conventions de code

- **Commits** : Conventional Commits (`feat:`, `fix:`, `docs:`, `style:`, `refactor:`, `test:`, `chore:`) avec scope. Ex : `feat(dtv): add multi-step form stepper`.
- **Composants** : Astro components (`.astro`) en `PascalCase`, îlots interactifs minimalistes (vanilla JS ou framework léger seulement si nécessaire).
- **Styles** : design tokens en variables CSS (voir `docs/STYLEGUIDE.md`), Tailwind v4 mappé sur ces tokens. Pas de couleurs en dur.
- **i18n** : aucune string en dur dans les composants — tout passe par les messages de traduction.
- Détails complets dans `docs/STYLEGUIDE.md`.

## Pointeurs de contexte

| Fichier | Contenu |
|---------|---------|
| `docs/PRD.md` | Product Requirements complet (vision, scope, data model, SEO, sécurité) |
| `docs/PLAN.md` | Plan d'exécution maître (epics + statuts) |
| `docs/DESIGN.md` | **Système design complet** (essence, voix, couleurs, typo Satoshi/Fraunces, motifs, composants) |
| `/styleguide` | Page live interne (noindex) — référence visuelle vivante |
| `docs/STYLEGUIDE.md` | Conventions de **code** (pointe vers DESIGN.md pour le visuel) |
| `docs/DECISIONS.md` | Log des décisions techniques |
| `docs/features/*.md` | Détail par feature/epic |
| `wildcat/project/` | **Bundle Claude Design** — maquettes HTML/CSS de référence (à recréer pixel-perfect) |
| `wildcat/project/Wildcat Landing.html` + `landing.css` | Direction visuelle **verrouillée** (page d'accueil de prod) |
| `wildcat/project/Wildcat Mega Menu.html` + `meganav.css` | Architecture de navigation + silos SEO |
| `wildcat/project/Wildcat Brand Board.html` | 3 directions explorées (contexte, A/B/C) |

## Design — direction verrouillée

Palette **"Tropical Garden"** (Direction B) + typo Satoshi / Fraunces :
- **Couleurs** : échelle verte `forest-light #3B6645` / `forest #2E5D3C` / `forest-deep #21422C` ; gold `#E0A62B` (gold-deep `#C98A12`) ; cream `#F6F1E6` ; orange/terracotta `#D97732` ; charcoal `#111`.
- **Typo** : **Satoshi** Bold (titres `.display/.h1/.h2/.h3`) + Satoshi (corps) ; **Fraunces 72pt SemiBold Italic** (accents or, classe `.em`). Lockup signature : *Come for the fight / Stay for the family*.
- **Icônes** : set unique **Tabler** via `astro-icon` (`<Icon name="tabler:..." />`) + logos `tabler:brand-*`. Custom : swoosh (`ArrowWild`), tigre, DTV stamp, ◆.
- **Marque** : toujours **Wildcat** (jamais de C majuscule).
- **Vibe** : open-air, golden hour, garden & pool. Photographie candide et dorée, jamais "warrior gym" sombre/agressif.
- **Source de vérité design : `docs/DESIGN.md` + page live `/styleguide`.** Tokens : `src/styles/tokens.css`. Conventions code : `docs/STYLEGUIDE.md`.

## État actuel

**Phase : E1→E5 complets + satellites DTV (E5b) + silo Train (E5c) + fighters guest book (E5d) + silo About (E5e) + silo Stay & Train (E5f). Prochain : contenu studio, vérif faits [À VÉRIFIER], puis E6 (DTV app).**

- [x] PRD lu et analysé · bundle de design analysé · docs projet créés
- [x] Décisions tranchées : langues **EN+FR** (V1), **Coaches + Fighters**, **prix publics**, **Astro 6**, **icônes Tabler**, **marque Wildcat**
- [x] **E1 — Scaffold** Astro 6 + Tailwind v4 : tokens, i18n EN/FR (`/en` `/fr`), adapter Vercel + sitemap, assets
- [x] **Design system** : palette verte 3 tons + gold/cream/charcoal/orange ; typo Satoshi + Fraunces + titres canoniques `.display/.h1/.h2/.h3` ; **boutons** (gold/forest/outline/link + flat + swoosh `ArrowWild`) ; **nav/mega menu** + drawer + utility bar ; **icônes Tabler** (`astro-icon`) ; focus-visible + reduced-motion ; `docs/DESIGN.md` + `/styleguide`
- [x] **E2 — App shell & design-system completion** : couleurs sémantiques (`--success/--warning/--error` harmonisées) ; `BaseLayout` complet (OG/Twitter, slot `jsonld`, preload satoshi-700, Nav+Footer) ; **`Footer`** 4 colonnes ; primitives **`Container`/`Section`** (+ `--gutter`/`--section-y`/`--maxw-narrow`) ; **système de formulaire** (`.wc-control` + `Field/Input/Textarea/Select/Checkbox/FileUpload`) ; **`Card`** de base ; **`src/config/site.ts`** = source unique contenu transverse (contact réel, Instagram/Facebook réels, **pricing THB réel**, **planning réel 7j/7** (`site.schedule`, flyer) ; email `TODO(real data)`). `npm run build` ✅
- [x] **E3 — i18n** : **Paraglide JS 2.x** (plugin Vite, strategy `url > globalVariable > baseLocale`, middleware SSG `setLocale`) ; source `messages/{en,fr}.json` (**144 clés, parité vérifiée**), output `src/paraglide/` gitignored ; **zéro string UI en dur** (Nav ~115 clés, Footer, BaseLayout, home, FileUpload) ; `site.ts` = données brutes only (listes à clés `dayKey`/`key` mappées par records explicites) ; **`LangSwitcher`** (préserve le path, aria-current, zéro JS) ; conventions dans `docs/STYLEGUIDE.md` ; décision + pièges dans `docs/DECISIONS.md`. `npm run build` ✅ (`/fr` rend FR, `/en` rend EN, bundle client inchangé)
- [x] **E4 — Sanity** : **studio embarqué `/studio`** (`@sanity/astro`, hash router en SSG, React pour le studio only) ; **6 schémas** (`sanity/schemaTypes/` : coach, fighter, blogPost, trainingSchedule, testimonial, category) + objets **`localeString`/`localeText`** (field-level {en, fr}) ; **blogPost en document-level** (`@sanity/document-internationalization` → slugs traduits, **Q6 réglée**) ; client `src/lib/sanity.ts` (`sanity:client`, `useCdn:false`) + `image.ts` (`urlFor`) + `i18n-content.ts` (`pickLocale`) + `queries.ts` (GROQ `defineQuery`) ; **typegen** `npm run sanity:types` (local, sans login) → `sanity.types.ts` committé ; robots.txt `Disallow: /studio` + sitemap filter. **Projet `etmrqgb2`** créé, dataset `production` public, CORS localhost OK, studio vérifié (login screen, zéro erreur d'hydratation). ⚠️ Env : **`PUBLIC_SANITY_PROJECT_ID`/`PUBLIC_SANITY_DATASET`** — `sanity.config.ts` est bundlé client, jamais de `process.env` dedans. `npm run build` ✅
- [x] **E5 — Pages cœur (prioritaires)** : **7 pages EN+FR** — Home (port fidèle de `Wildcat Landing.html` sur le thème, **zéro import de landing.css**), `/dtv-visa` (pillar, JSON-LD FAQPage, #eligibility #faq), `/stay-train` (cross-sell DTV), `/classes` (#schedule #pricing, **prix publics complets**), `/fighters` (CMS-only, coming soon si vide), `/about` (#coaches), `/contact` + **`/api/contact`** (seule route on-demand : `prerender=false`, fetch REST Resend gardé par `RESEND_API_KEY`, honeypot). ~20 composants sections build-as-you-go (Hero, PageHero, DtvTeaser/Steps/Stamp, Program/Coach/Fighter/Pricing cards, ScheduleTable, TestimonialQuote, Faq `<details>`, CtaBanner, Breadcrumb, FeatureGrid, ContactForm…). **`safeQuery`** (`src/lib/content.ts`) + fallbacks messages → build vert dataset vide ; `localePath()` (`src/lib/routes.ts`) pour tous les liens (slugs EN dans toutes les locales). Nav/Footer re-pointés (clusters → pillar+anchor, zéro 404 ; Blog `#` jusqu'à E8). **466 clés messages, parité EN/FR vérifiée.** JSON-LD : SportsClub (home), FAQPage (dtv), BreadcrumbList. `npm run build` ✅
- [x] **E5b — Satellites DTV (2026-06-04, branche `feat/dtv-satellites`)** : **5 pages EN+FR data-driven** — recherche **DataForSEO** (EN/US, `.seo-data/keywords-*.json`) + **scans SERP** (×5 sub-agents, `.seo-data/serp-*.json`) + **briefs Module C YMYL** (`content/_drafts/pages/`) → `/dtv-visa/eligibility` (+**DtvQuiz** 6 Q → 3 verdicts), `/dtv-visa/how-to-apply` (+**DocChecklist** localStorage, **HowTo JSON-LD** 7 steps), `/dtv-visa/muay-thai` (table **DTV vs ED vs Tourist**, kw "muay thai visa" diff 0), `/dtv-visa/long-stay-training` (+**BudgetEstimator** prix `site.ts`), `/dtv-visa/faq` (30 Q / 7 thèmes +**FaqSearch**, **FAQPage JSON-LD déplacé du pillar** — 1 seul par silo). Interactifs : `src/components/interactive/`, **vanilla `<script>` IIFE + `data-*`** (pattern Nav), zéro island, progressive enhancement, a11y (aria-live, reduced-motion). Contexte SEO créé : **`docs/identity.md` + `docs/voice.md` + `docs/seo-context.md`** (auteur E-E-A-T : **Meaw Boonpradub**, owner). **1029 clés messages (parité OK)**, Nav/drawer/pillar remaillés (zéro ancre), tests **Playwright 26/26** (`tests/e2e-interactives.py`). `npm run build` ✅
- [x] **E5c — Silo Train (2026-06-04, branche `feat/train-cluster`)** : **architecture data-driven 9 pages → 2** (`docs/structure-train.md`, DataForSEO US/UK/FR ×6 + scans SERP ×2 — women/kids/all-levels/private/schedule/pricing invalidés, 0 volume) → **`/classes/beginners`** créé (kw "beginner muay thai classes" **1000/mo diff 0**, variante A, **byline Meaw + Article JSON-LD**, walkthrough minute-par-minute 5 H3, +**FirstClassChecklist** = `DocChecklist` réutilisé via prop `storageKey`, 106 clés `classes_beg_*`) + **re-cadrage pillar `/classes`** (7 modifs : variante A, cards enrichies + Beginner→satellite/Fight Team→`/fighters`, section `#camp` dark, transparence pricing + liens long-stay, FAQ → 5 PAA SERP — l'ex-FAQ DTV/scooter migrée sans perte). Nav "Beginner" → satellite (seul changement menu) ; maillage home/stay-train/eligibility. Pipeline : 2 sub-agents rédaction parallèles → checkpoint → intégration → reviews. **1165 clés (parité OK)**, `/seo-review` ×2 PASS, **Playwright 51/51**. ⚠️ Insight FR : "camp/stage muay thai thailande" (210+140/mo) → backlog Stay & Train batch 2. `npm run build` ✅
- [x] **E5d — Fighters « livre d'or » (2026-06-04, branche `feat/train-cluster`)** : re-cadrage `/fighters` en **guest book** (les guests qui ont combattu à Chiang Mai grâce à la famille — **pas de record W/L**, la stat = combats pris ; pas de status current/alumni). **Schéma fighter migré** : −status/wins/losses/bio/nationality, +`fightName`, `countryCode` (ISO-2), `year`, `venues` (4, checkboxes), `quote` (localeString), **`achievements` (10 valeurs figées, checkboxes studio)**. Design **polaroid** (4 directions explorées sur `/styleguide`, F·B validée par Jonathan) : **`FighterCard`** réécrit (tape gold, rotation seedée, quote Fraunces, max 3 stamps, fallback initiales+tigre, a11y sr-only) + **`AchievementStamp`** (tampon CSS fond papier, 3 encres ; **`STAMP_ASSETS` stub** → webp encre générés par Jonathan, rendu multiply, prompts fournis) + **`src/lib/guestbook.ts`** (ACHIEVEMENT_VALUES = priorité, seedRot, flagOf — ⚠️ emoji drapeaux KO sur Chrome/Windows, dégrade en lettres). Page : **compteur agrégé build-time** (membres/combats/nationalités, **affiché dès 3 membres**), grille punaisée. **1179 clés (parité OK)**. ✅ 10 assets stamps encre livrés (pipeline die-cut `tests/process-stamp.py` : trim+flood blanc→alpha+webp). **Design final = « incrusté scrapbook » v2 (2026-06-05)** : photo posée sur le fond, **bord déchiré seedé** (2 frames carrés rasterisés `tests/process-frame.py` ×4 rotations ×2 miroirs CSS = 16 variantes, photo contre-tournée), scotch (3 assets, côté seedé), stamps **en grappe côté opposé au scotch** (max 3, 15 %/stamp, zigzag), ornement nom + **drapeau SVG `flag-icons` inliné au build** (emoji KO sur Chrome/Windows), **quote manuscrite Caveat** (`--font-hand`, Fontsource), tout l'aléatoire seedé sur le nom. Vérifié en réel (1er fighter saisi au studio).
- [x] **E5e — Silo About (2026-06-04, branche `feat/train-cluster`)** : fin du menu "tout pointe sur /about" — **`/about/coaches`** créé (page équipe + **entité auteur E-E-A-T** : bloc Meaw 100 % message-driven, **Person JSON-LD `#meaw`** = cible canonique des `author.url` Article/HowTo + bylines cliquables des 5 satellites + footer), `/about` re-travaillé (coaches → **teaser** `#coaches` conservé, `#community`, **section `#reviews`** : grille de tous les testimonials featured + CTA `site.reviews.googleUrl` TODO real data), **PageHero variante photo** (props `image`+`eyebrow`+`image.position`, **direction D2 « poster inversé » compactée (2026-06-05)** : photo intacte `clamp(280px,42vh,460px)` + brush + bande forest — l'overlay direction B était illisible) ; **heroes v3 (2026-06-05)** : `/about` = **`SplitHero`** (direction C : texte cream + photo bord droit + callout float, photo soirée `family-party.webp`, +2 clés float), `/about/coaches` = **PageHero photo direction A compacte** (overlay gradient + brush bas fill cream, `hero-team.webp`), home = `hero-home.webp` + **`.display` hero agrandi** (`clamp(46px,7vw,92px)`, inner 1060px) ; **photos réelles (zéro doublon)** : Why Wildcat → `gym-place.webp` (enseigne), bloc Meaw → `meaw-training.webp` (meaw-3 action), stay-train → `night-session.webp` ; réserve `PNG/` : team-krus, family-about, 🫶, mega About re-pointé (coaches→page, community/reviews→ancres, **Gallery retirée jusqu'à E9**, drawer parité). **1223 clés (parité OK)**, Playwright 51/51. ⏳ portrait Meaw (hero-team.webp en attendant).
- [ ] Questions ouvertes : **Q1** storage DTV (avant E6). ✅ **Q5 domaine tranché (2026-06-05)** : `wildcatmuaythai.com` (+ `wildcatchiangmai.com` redirect défensif) — **achat à faire**. Env prod à poser : `RESEND_API_KEY`, `CONTACT_EMAIL`. Redirect `/muay-thai-training` → `/stay-train` (E7/E10). Faits `[À VÉRIFIER]` avant prod : ⏳ visa DTV (**fact-check en cours** → `docs/dtv-fact-check.md`) · ✅ adresse confirmée (Nong Kwai, Hang Dong — copy camp ancrée + **map embed `/contact`** via `site.contact.mapEmbed`) · ✅ gants+bandes prêtés confirmés · ⏳ âge mini kids (~6 ans estimé, à valider avec Meaw — aucun âge publié).
- [x] **Hero home tranché (2026-06-05)** : direction actuelle conservée (`hero-home.webp` + `.display` agrandi) — seule l'image pourra être upgradée en meilleure qualité.
- [x] **Polish heroes + CTA (2026-06-05, branche `feat/train-cluster`)** : **overlays photo passés en charcoal `#111`** (hero home + `PageHero` variante photo + démo A styleguide ; gradient 0.6/0.32/0.65/0.92, voile gold conservé — verdict Jonathan : « le noir rend mieux que du green, sinon ça fait trop green partout », l'ancienne doctrine "jamais de noir" ne vaut plus pour les overlays) ; **fix espace `title`/`titleEm`** avalé par le compilateur Astro dans les expressions (`{' '}` explicite — bug visible sur les 3 variantes `CtaBanner`, appliqué en préventif à PageHero/SplitHero/DtvTeaser/StayTrainUpsell).
- [x] **Cards programme & pricing (2026-06-05, branche `feat/train-cluster`)** : explorations 4 directions sur `/styleguide` (relief, ticket, poster sombre, scrapbook — écartées, historique git) → **`ProgramCard` « incrustée scratch »** : zéro ombre/bordure/radius, carte entière masquée par les frames déchirées du guest book (2 assets × 2 miroirs CSS, contre-flip du contenu, seed sur le titre), transition déchirée photo→carte (assets TornDivider), niveau manuscrit Caveat, `ArrowWild`, **pas d'effet hover** (cursor only) — ⚠️ **fonds cream/soft uniquement** ; **`PricingCard` « scrapbook »** : coches Caveat, prix cerclé au marqueur (populaire), tampon **`decor/stamp-most-popular.webp`** (asset Jonathan via pipeline die-cut, rendu **opaque sans multiply**, rotation seedée sur le nom, alt = `popularLabel` localisé), **aucune bordure colorée**. API inchangées → zéro modif pages (home/classes/stay-train stylées d'office). Styleguide : protos remplacés par la démo des composants réels ; `Card` de base conservée (cartes texte simples). Outil : `tests/shot-cards.py` (re-screenshots cards).
- [x] **E5f — Silo Stay & Train (2026-06-05, branche `feat/train-cluster`)** : pipeline data-driven (`docs/structure-stay.md`) — DataForSEO ×7 US+FR : **"muay thai camp thailand" 1900/mo diff 29 LOW découvert** (la page visait "training camp chiang mai" ~10/mo), **5 satellites maquette invalidés → 0 page créée** (accommodation 0, holiday 10, stay-and-train 0, scooter hors silo, séjour FR 0) ; scans SERP ×3 (gaps 8/10 : page 1 = fight camps elite Phuket/Bangkok, prix cachés, DTV jamais expliqué ; FR = listicles + agences à marge). **Re-cadrage majeur du pillar `/stay-train`** (brief `stay-pillar.md`, variante B "Packages & Prices") : EN tête nationale, FR ré-anglé "camp/stage" (H1 camp + stage en H2, angle réservation directe sans marge — **claim "accueil en français" interdit**, équipe anglophone) ; sections **#packages** (ex-#pricing, table inclus/à organiser), **#accommodation** (**condos de Meaw** — Q-A PRD tranchée 2026-06-05), **#life**, **FAQ 6 PAA + FAQPage JSON-LD** (porteur unique du silo Stay) ; composant `Faq` étendu (prop `link` par item) ; Nav 6 entrées mega + drawer re-pointées (**Long-Stay → `/dtv-visa/long-stay-training`**) ; maillage 6 cibles (beginners, classes#schedule, dtv ×2, about/coaches, contact). **1236 clés (parité OK)**, audit SEO EN+FR vert, **Playwright 69/69** (+18 tests Stay, `WC_BASE` paramétrable pour cibler un serveur statique). ⏳ avant prod : détail "what's included" packages + condos (prix/distance) avec Meaw, photos #accommodation/#life. Backlog blog E8 : repris dans le calendrier éditorial (`docs/structure-blog.md`).
- [x] **Satellite Scooter Rental + checklist Meaw (2026-06-05, branche `feat/train-cluster`)** : **`docs/checklist.md`** = inventaire exhaustif de tout ce qui dépend de Meaw (DTV §A, packages §B, condos §C, **scooters §D**, cours §E, admin §F, studio §G, photos §H, divers §I + top 10 final). **Verdict scooter E5f renversé** (Meaw loue réellement : Scoopy i Prestige 2024 350/j · 3000/mois + Click) → **`/stay-train/scooter-rental`** (kw "scooter rental chiang mai" 210/mo diff 24, angle loueur au camp Hang Dong) : **collection Sanity `scooter`** (prix THB, photo, note localeString, **toggle `available`**, `SCOOTERS_QUERY`) + fallback **`site.scooterFleet`** (Click prix TBD → "ask us"), fleet cards badge dispo, how-it-works + FAQ **prudents** (caution/casque/permis = "ask when you book" tant que checklist §D ouverte), **Service JSON-LD** (offers 350/3000 — pas de FAQPage, porteur = pillar). Nav ×3 re-pointées (mega, drawer, foot DTV) + maillage stay-train#life/long-stay. **1295 clés (parité OK)**, audit SEO EN+FR PASS (metas 147/144, H1 + Chiang Mai), build ✅.
- [x] **Calendrier éditorial Blog E8 (2026-06-05)** : `docs/report-kimi.md` analysé (verdict ~30 % utile : zéro data keyword, écrit sans connaître le site, mais 3 pépites) + **validation DataForSEO ×16 clusters** (burning season ~900/mo cumulé diff 6 🏆, benefits 210, weight loss 170, wai kru 260, ed visa 720, mongkhon 590, vs kickboxing 2900, stance 4400 ; invalidés : assurance/santé mentale/nomad en dédié, bootcamp/coaching privé hors offre, lead magnets hors V1). Agrégation **575 kws ≥10/mo** (50 fichiers `.seo-data/`) → **`docs/structure-blog.md`** : 6 mois × 6 clusters × 24 articles + réserve, règles (10/mo = article OK, 0 → alternative comparaison, zéro cannibalisation pillars, cluster/mois), pipeline production, catégories Sanity proposées. ⚠️ Burning season (M4) en ligne **avant décembre**. **Stack blog tranchée + audit SEO technique (2026-06-05)** : blog = **Astro Content Collections `.md`** (renverse Q6/E4 — pipeline skills 1 article = 1 commit, Portable Text inadapté aux tableaux, Meaw hors scope blog ; schéma `blogPost` + plugin doc-internationalization **à retirer en E8** — `DECISIONS.md`) ; i18n blog = slugs traduits par fichier + `translationKey` frontmatter, ⚠️ **piège hreflang** : BaseLayout/LangSwitcher supposent des paths symétriques → prop `alternates` à câbler en E8 AVANT le 1er article (`structure-blog.md` §7) ; audit SEO technique : hreflang+x-default/sitemap i18n/canonical/og:locale **déjà en place**, quick wins appliqués (geo + priceRange dérivé de `site.pricing` sur SportsClub, `inLanguage` FAQPage/HowTo, filtre sitemap `/api/`).
- [x] **Système d'intake contact (2026-06-05, branche `feat/train-cluster`)** : refonte conversion à 3 étages. **`ContactForm` v2 intent-driven** — 5 intentions en radio-cards (class/stay/scooter/dtv/general, icônes Tabler), champs contextuels par intention togglés en **pur CSS `:has()`** (no-JS OK ; jamais `required` — un champ caché requis bloquerait le submit ; l'API ne lit que les champs de l'intention active), présélection **`?intent=`** (+ legacy `?topic=`) par script, **fast-lane WhatsApp pré-rempli** re-câblé par intention. **`src/lib/whatsapp.ts`** (`waLink(text)` → `wa.me?text=`) — ⚠️ textes pré-remplis **EN dans les 2 locales** (équipe anglophone, cohérent E5f). **`/api/contact` v2** : champs extra whitelistés par intention → **email pré-qualifié** (`[Wildcat] {intent} — {name}`), message libre requis pour `general` only, redirect `from` whitelisté (contact, stay-train→`#plan`). **`StayInquiryForm` inline `#plan`** sur `/stay-train` (arrivée/durée/personnes/logement?/scooter?, bannières sent/error, CTAs locaux + pricing cards → `#plan`, `#life` passé soft). **Scooter WhatsApp-first** : wa pré-rempli partout + **par modèle sur les fleet cards** (`scooter_wa_model({model})`), secondaire → `?intent=scooter`. **Sweep ~40 CTAs** : classes/beginners/about/coaches/home → `?intent=class` (cards programmes 2-5 de `/classes` en CTA explicite « Book this class » — fin du learn-more déguisé), pages DTV → `?intent=dtv` (fix `?topic=dtv` mort — jamais lu par le form v1), Nav `ctaHref` suit le label contextuel (dtv→intent=dtv, stay→`/stay-train#plan`, class/join→intent=class). **1370 clés (parité OK)**, build ✅.
- [x] **Backend forms — Web3Forms branché (2026-06-06)** : POST direct des 2 forms (`ContactForm`, `StayInquiryForm`) vers `api.web3forms.com/submit` — `access_key` = **`PUBLIC_WEB3FORMS_KEY`** (clé publique, .env + à poser sur Vercel), `redirect` = URL absolue `Astro.site` → `?sent=1` (bannières existantes inchangées), `subject` pré-qualifié par intention (JS, fallback générique no-JS), `from_name`, honeypot **`botcheck`** natif. Filtrage des champs par intention re-implémenté côté client : `disable` des `.xtra` non actifs au submit (no-JS = tous les champs partent, verbeux mais OK). **`/api/contact` supprimée → site 100 % statique** (build `mode: "static"`, zéro serverless) ; env Resend caducs. ⚠️ Les emails arrivent sur la boîte liée à la clé Web3Forms ; pas de page d'erreur custom (échec = page d'erreur Web3Forms).
- [x] **Audit pré-prod V1 + correctifs (2026-06-06, master)** : audit complet ×4 sub-agents (liens/ancres/hreflang/sitemap **clean**, coordonnées+JSON-LD réels, comptes Sanity : 0 coach/0 testimonial/1 fighter/0 scooter). Correctifs : **5 articles démo → `draft: true`** (bannière « Article de démo » était publiée) ; **fallbacks factices retirés** (Kru Nut/Fern/Bank ×3 pages, James T./Sofia R./Marc D. ×2 — sections CMS-only masquées si vide ; `/about#coaches` et `#reviews` gardent leur shell, ancres nav préservées ; clés messages fallback dormantes dans les JSON) ; lien mort « application form » (how-to-apply, promesse E6) → `?intent=dtv` + copy reformulée EN/FR ; **sitemap : `/styleguide` exclu** ; `site.ts` : email confirmé, `googleUrl` → recherche Google (pas d'accès GMB, TODO lien direct fiche) ; **`docs/photos-needed.md`** = shopping list photos P1-P3 (condos, vie camp, scooters, portrait Meaw, coachs, ambiance CTA, OG). Reste avant mise en ligne : contenu studio + env Vercel (`PUBLIC_SANITY_*`, `APIFY_TOKEN`, `PUBLIC_WEB3FORMS_KEY`) + achat domaine.
- [x] **Moteur blog E8 (2026-06-05, branche `feat/train-cluster`)** : blog 100 % prêt, il ne manque que les articles. **Content Collection** `src/content/blog/{en,fr}/slug.{md,mdx}` (`src/content.config.ts` : zod, `translationKey`, `draft` filtré prod, cover `astro:assets` co-localisée `covers/`, 6 catégories = clusters) + **`src/lib/blog.ts`** (getPostsByLang/getAlternates/getRelated/getCategoryCounts). **`/blog`** : grille **`BlogCard`** (« éditoriale cousue » validée Jonathan : Card de base + catégorie Caveat + extrait clamp 2 + **couture déchirée** seedée — explorations 4 directions historique git) + **`BlogFilters`** (chips catégories non vides × recherche, pattern FaqSearch, `?category=` partageable via replaceState) + état vide pattern fighters. **`/blog/[slug]`** : header éditorial (catégorie cliquable, byline Meaw, dates), **`.wc-prose`** (global.css — tableaux scrollables, raison du choix .md), **`BlogToc`** (`<details open>`, H2 via `headings` de `render()`, sticky ≥1140px), `ShareButtons` (copy PE + wa/X/FB natifs), `AuthorBio` E-E-A-T, **`ArticleQuiz`** (mi-contenu **.mdx only** — intégration `@astrojs/mdx`, no-JS = réponse en `<details>`), **`HelpfulWidget`** (oui→localStorage ; non→textarea→**Web3Forms**, 1er composant dessus, `PUBLIC_WEB3FORMS_KEY` absente = merci sans fetch ; hook `window.wcTrack`), related 3 (même catégorie), **BlogPosting JSON-LD** (author `@id #meaw`, isPartOf Blog). **Piège hreflang §7 câblé** : prop `alternates` BaseLayout→Nav→LangSwitcher (slugs traduits croisés ; EN-only = en+x-default seuls, switcher → `/fr/blog` ; x-default omis si pas d'EN ; pages service inchangées) + **sitemap `serialize`** (retire les `xhtml:link` des articles, l'index garde les siens) ; **RSS par locale** `/{lang}/blog/rss.xml`. Nav (mega+drawer) + Footer câblés (fin du `#`). **Sanity purgé** : −blogPost −category −plugin doc-internationalization (npm aussi), −queries BLOG_*/CATEGORIES, re-typegen (6 queries/19 types). **3 articles sample « démo E8 »** (burning-season EN+FR tableau+cover · women EN+FR .mdx+quiz · stance EN-only) = squelettes à remplacer par le pipeline `/seo-brief → /seo-write`. ⚠️ fix watcher `'**/content/**'`→`'content/**'` (matchait `src/content/` → HMR mort). **Itérations template (2026-06-06, validées Jonathan)** : **TLDR requis** (frontmatter `tldr` zod 2-5 puces — `/seo-write` en produit toujours) rendu en **`Callout` brief/stripe** sous la byline ; header article **aligné à gauche** (largeur Container, même bord que breadcrumb/prose) ; **TOC sticky à droite** (le contenu prime, DOM inchangé = mobile TOC en haut) ; **cover retirée du corps d'article** (jobs restants : card grille + og:image/JSON-LD — image d'ambiance ponctuelle = dans le markdown) ; **photo auteur résolue du studio** (fiche coach « Meaw » via `COACHES_QUERY`+`urlFor`, fallback initiales, injectée aussi dans `author.image` JSON-LD — pas de schéma author dédié). **1434 clés (parité OK)**, build ✅.
- [x] **Batch photos 1 + reviews DTV + polish visuel (2026-06-06, master)** : 27 images livrées par Jonathan triées (`PNG/image-a-traiter/`, pipeline `tests/process-batch-photos.py`) — **6 cards programmes `/classes` + 3 home en photos réelles** (prop `image.position` ProgramCard, zone photo 190→232px), `camp-life.webp` sur `/stay-train#life`, **portrait Meaw réel** (fiche + Person JSON-LD). **Reviews WhatsApp** (11 approvals DTV) : captures = PII (passeports/e-visas) **gitignorées**, → **8 transcriptions anonymisées EN+FR prêtes à coller** dans `docs/studio-content.md` (runbook saisie studio complet : testimonials + coachs Peter/Arun + fighters). **Affichage étendu** : home (quote manuscrite Caveat on-dark dans DtvTeaser, prop `quote`), how-to-apply (grille 2), eligibility (1), `/about#reviews` service→null (mix), anti-doublon home (#coaches = 1re non-dtv) ; clés `dtv_reviews_*`. **Polish** : SplitHero `/about` plein viewport (100svh−112px, slot `crumbs`+`Breadcrumb overlay`, −encart float), photo Why Wildcat « incrustée scratch » (mask torn-frame), hero home 81vh (juste milieu). ⚠️ Trouvaille : collage stay-train.jpg révèle **Fighter Program 28 000 THB avec scooter** (checklist §B) ; manquent photos condos originales + scooters. **1436 clés (parité OK)**, build ✅.
- [x] **Audit responsive mobile (2026-06-07, master)** : nouveau **`tests/responsive-audit.py`** rejouable (17 pages × EN/FR × 360/390/768px, détection overflow horizontal + éléments fautifs + screenshots, `WC_BASE` paramétrable) — **8 FAIL → 108/108 PASS** + revue visuelle ×4 sub-agents (34 screenshots 390px). **7 fixes** : Nav util bar (règle `.hide-sm` de la maquette jamais portée → la banner cassait le scroll mobile), `Button` (`max-width:100%` + wrap ≤480px — un libellé nowrap débordait), `BudgetEstimator` (`min-width:0` — les selects gonflaient la grid), fighters (stamps −12px mobile + clip viewport, l'overhang reste visible dans la gouttière), blog `[slug]` (`min-width:0` sur la grid — la table markdown `width:max-content` gonflait la track), `ContactForm` encart WhatsApp (écrasé à 1 mot/ligne → texte pleine largeur, bouton dessous), `DocChecklist` (badge « We prepare these » chevauchait le titre wrappé → `flex-wrap`), tables `.wc-prose` (ombres de scroll + fonds cellules 82 % pour les laisser transparaître). ⚠️ **Piège dev découvert** : dev server long-running → locale Paraglide gelée EN sur `/fr/*` (double instance runtime via HMR, **build prod correct**) — restart suffit. Signalements contenu : typo « Doiminic » (testimonial studio), coach sans photo = tape orpheline home. Build ✅.
- [x] **Passe motion site entier (2026-06-07, branche `feat/motion-pass` — NON MERGÉE)** : infra scénographie (tokens `--dur-*`/`--ease-*`, `src/scripts/animations.ts` + `src/styles/motion.css`, hidden-state gated `html.wc-anim` → no-JS/reduced-motion = visible, démo `/styleguide#motion` + replay) ; **heroes = entrées chorégraphiées au load** (`data-animate-load` : home line-rise lockup ~1.7s, PageHero/SplitHero/header article ~1s) ; staggers/reveals sur tous les composants partagés (grids Program/Pricing/Coach/Feature/IG/Approvals/Community, TestimonialQuote blur via wrapper, CtaBanner ×3, DtvSteps timeline) → **site entier couvert par héritage** ; `/fighters` **parallax 3 vitesses CSS scroll-driven natif** (`animation-timeline: view()`, zéro GSAP, `@supports` + desktop only). Statiques voulus : nav/footer/breadcrumb/FAQ/tables/forms/Callout. ⚠️ Pièges gravés (`DECISIONS.md` + skill `/motion` v1.1) : **transition sur `.in-view` only** (sinon fade-out à l'init qui tue les reveals au load), reflow forcé avant reveal synchrone, rootMargin -18%, animation+transition jamais le même transform (2 wrappers), cards à transform seedé = wrapper. Skills : `/motion` v1.1 (assets canoniques + « Standard ≠ uniforme »), `/init-design` aligné, `/animate` supprimé.
- [ ] Prochain : **QA motion (fresh-eyes Jonathan + `/impeccable critique` ×2-3 pages) puis merge `feat/motion-pass` → master** ; saisir le vrai contenu dans le studio (**testimonials DTV transcrits + coaches + fighters — runbook `docs/studio-content.md`**/schedule/**scooters**) ; passer `docs/checklist.md` avec Meaw ; corriger les faits DTV selon `docs/dtv-fact-check.md` ; **produire les articles E8** (calendrier `docs/structure-blog.md`, M4.1 burning season avant décembre — remplacer les 3 samples démo) ; puis **E6 DTV app** (trancher Q1 storage avant)

_Dernière mise à jour : 2026-06-07 (passe motion)._
